```python
class cat_Node:

    #через ключевое слово self объявляются ПОЛЯ класса,
    #т.е. переменные, которые будут принадлежать его экземпляру.

    def __init__(self, n = None):
        self.prev = None
        self.next = None
        self.name = n

    def __str__(self):
        return self.name

class cat_LinkedList:

    def __init__(self):
        self.head = None
        self.tail = None

    def __str__(self):
        pass
    
    def add(self, cat_name):

        #Мы создаем переменную, в которой будем хранить ссылку на новый
        #созданный узел. При его создании, мы передаем информацию - имя кота.

        newCat = cat_Node(cat_name)

        #Если указатель head для списка пустой, то мы указываем
        #нашу ссылку newcat в качестве указателя (строки 25, 26)
        if self.head is None:
            self.head = newCat

        #Иначе, если указатель уже есть, мы объявляем переменную current и храним
        #в нем ссылку на "голову" нашего списка. Затем перебираем все элементы
        #списка до тех пор, пока указатель на следующий элемент не станет пустым.
        #И добавляем в качетсве пустого ссылку на новый элемент.
        else:
            current = self.head
            while current.next is not None:
                current = current.next
            current.next = newCat

CATS = cat_LinkedList()

CATS.add("Барсик")
CATS.add("Соня")
CATS.add("Гуманоид")
CATS.add("Ирокез")
CATS.add("Добрый")

print(CATS.head)
```

Я понял как создается переменная, как объявляются поля класса(self), не совсем понял как работает head и как работает переменная current. Данный код предоставляет функциональности создания двусвязного списка на примере котов. Мы описали класс cat_Node для узлов списка