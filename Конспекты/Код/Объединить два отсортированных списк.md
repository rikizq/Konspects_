#Код #leetcode #Python 
```python
#Зимин Я.В.

class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        res = ListNode()
        head = res
        t = None
        while list1 and list2:
            if list1.val <= list2.val:
                res.val = list1.val
                list1 = list1.next
            else:    
                res.val = list2.val
                list2 = list2.next
            res.next = ListNode()
            t = res
            res = res.next
        while list1:
            res.val = list1.val
            list1 = list1.next
            res.next = ListNode()
            t = res
            res = res.next
        while list2:
            res.val = list2.val
            list2 = list2.next
            res.next = ListNode()
            t = res
            res = res.next
        if t:
            t.next = None    
        else:
            head = None
        return head
```

Я изучил, материал, что такое "Классы" В Python, как использовать ООП-объекты внутри алгоритма.
Как написать алгоритм слияния двух структур и как создаются и связываются узлы.

В строке номер 3 - создаётся пустой узел результата.
Он будет содержать значение первого элемента будущего списка
В строке номер 4 - сохраняется указатель на начало списка — его нужно будет вернуть.
В строке номер 5 - t позже будет указывать на **предыдущий узел** — нужен, чтобы убрать лишний пустой хвост.

Строки 7–12
Сравнение значений и копирование меньшего в текущий узел результата:
строка 8: res.val = list1.val
строка 11: res.val = list2.val

Строка 13
res.next = ListNode()
Создаётся новый пустой узел, который станет следующим элементом.

Строка 14 - запоминает предыдущий реальный узел результата.
Строка 15 - переход к новому узлу - он станет текущим.
Строка 17 - Копирует значение.
Строка 19 - создает новый пустой узел.
Строка 20-21 - обновляем и смещаем t.
Строка 28 - Если в списке был хотя бы один элемент, t указывает на последний заполненный узел.
Строка 29 - Лишний пустой узел, который всегда создается в конце.
Строка 30-31 - помогли доделать это (не сильно понял)
Строка 32 - Возвращается указатель на 1 элемент.

Все это делать помогли мне Северюхин М.В. и Репин А.А.