```python
import random

ANSWER = ['Да-ааа','Не-еее-т' 'Боги думают над этим вопросом...']

while True:
	x = input("Боги ждут ваш вопрос! Введите exit. \n")
	if x.find("exit") != -1:
		print("Пока пользователь.")
		break
	else:
		print(random.choice(ANSWER))
	
```