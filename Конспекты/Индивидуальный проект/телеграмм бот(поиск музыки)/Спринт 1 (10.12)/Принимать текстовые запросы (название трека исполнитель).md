```python
import requests
from aiogram import Bot, Dispatcher, types
from aiogram.filters import Command
from aiogram.types import Message
import random
import asyncio

TOKEN = "8205786674:AAF0JYnQBU7F6-hXQ0eqjYoJZyxAFhlxKsA"

bot = Bot(token=TOKEN)
dp = Dispatcher()


# --- Функции поиска ---
def search_music(query, limit=5):
    url = "https://itunes.apple.com/search"
    params = {
        "term": query,
        "entity": "song",
        "limit": limit,
        "country": "RU",
        "lang": "ru_ru"
    }
    return requests.get(url, params=params).json().get("results", [])


def search_by_artist(artist, limit=5):
    url = "https://itunes.apple.com/search"
    params = {
        "term": artist,
        "entity": "song",
        "attribute": "artistTerm",
        "limit": limit,
        "country": "RU",
        "lang": "ru_ru"
    }
    return requests.get(url, params=params).json().get("results", [])


def search_by_track(track, limit=5):
    url = "https://itunes.apple.com/search"
    params = {
        "term": track,
        "entity": "song",
        "attribute": "songTerm",
        "limit": limit,
        "country": "RU",
        "lang": "ru_ru"
    }
    return requests.get(url, params=params).json().get("results", [])


def get_top_tracks(limit=5):
    url = f"https://itunes.apple.com/ru/rss/topsongs/limit={limit}/json"
    try:
        results = requests.get(url).json().get("feed", {}).get("entry", [])
        tracks = []
        for t in results:
            tracks.append({
                "trackName": t["im:name"]["label"],
                "artistName": t["im:artist"]["label"],
                "previewUrl": t["link"][0]["attributes"]["href"]
            })
        return tracks
    except Exception as e:
        print(f"Error getting top tracks: {e}")
        url_us = f"https://itunes.apple.com/us/rss/topsongs/limit={limit}/json"
        results = requests.get(url_us).json().get("feed", {}).get("entry", [])
        tracks = []
        for t in results:
            tracks.append({
                "trackName": t["im:name"]["label"],
                "artistName": t["im:artist"]["label"],
                "previewUrl": t["link"][0]["attributes"]["href"]
            })
        return tracks


def get_random_tracks(limit=5):
    search_terms = ["а", "и", "love", "pop", "rock", "д"]
    term = random.choice(search_terms)
    results = search_music(term, limit=50)

    if len(results) < limit:
        results += search_music(random.choice(["a", "the", "me"]), limit=30)

    return random.sample(results, min(limit, len(results)))


# --- Команды бота ---
@dp.message(Command('start'))
async def cmd_start(message: Message):
    await message.answer(
        "Привет! Я музыкальный бот 🎵\n\n"
        "Напиши название трека или исполнителя, и я найду музыку для тебя.\n"
        "Я ищу как русские, так и международные песни!\n\n"
        "Используй /help чтобы узнать все команды."
    )


@dp.message(Command('help'))
async def cmd_help(message: Message):
    await message.answer(
        "Вот что я умею:\n"
        "/help - показать эту справку\n"
        "/random - получить случайные треки\n"
        "/top - показать топ-песни\n"
        "/settings - настройки бота\n"
        "/artist <имя> - поиск треков по исполнителю\n"
        "/track <название> - поиск конкретного трека\n\n"
        "Или просто напиши название трека или исполнителя."
    )


@dp.message(Command('random'))
async def cmd_random(message: Message):
    tracks = get_random_tracks()
    if not tracks:
        await message.answer("Не удалось найти случайные треки ")
        return

    text = " Случайные треки:\n\n"
    for t in tracks:
        text += f"🎵 {t.get('trackName', 'Неизвестно')}\n"
        text += f" {t.get('artistName', 'Неизвестно')}\n"
        if 'previewUrl' in t:
            text += f" {t['previewUrl']}\n"
        text += "\n"
    await message.answer(text[:4000])


@dp.message(Command('top'))
async def cmd_top(message: Message):
    tracks = get_top_tracks(limit=10)
    if not tracks:
        await message.answer("Не удалось загрузить топ-песни ")
        return

    text = " Топ-песни:\n\n"
    for i, t in enumerate(tracks, 1):
        text += f"{i}. 🎵 {t.get('trackName', 'Неизвестно')}\n"
        text += f"   👤 {t.get('artistName', 'Неизвестно')}\n"
        if 'previewUrl' in t:
            text += f"   🔗 {t['previewUrl']}\n"
        text += "\n"
    await message.answer(text[:4000])


@dp.message(Command('settings'))
async def cmd_settings(message: Message):
    await message.answer(
        " Настройки бота:\n\n"
        "• Страна поиска: Россия\n"
        "• Количество результатов: 5-10\n"
        "• Язык интерфейса: Русский\n\n"
        "Бот ищет музыку в iTunes Store с приоритетом российского региона."
    )


# --- Поиск по исполнителю ---
@dp.message(Command('artist'))
async def cmd_artist(message: Message):
    args = message.text.split(maxsplit=1)
    if len(args) < 2:
        await message.answer(" Пожалуйста, укажи имя исполнителя после команды /artist")
        return

    tracks = search_by_artist(args[1], limit=10)
    if not tracks:
        await message.answer(f"Исполнитель '{args[1]}' не найден ")
        return

    text = f" Треки исполнителя {args[1]}:\n\n"
    for i, t in enumerate(tracks[:10], 1):
        text += f"{i}. 🎵 {t.get('trackName', 'Неизвестно')}\n"
        if 'previewUrl' in t:
            text += f"    {t['previewUrl']}\n"
        text += "\n"
    await message.answer(text[:4000])


# --- Поиск конкретного трека ---
@dp.message(Command('track'))
async def cmd_track(message: Message):
    args = message.text.split(maxsplit=1)
    if len(args) < 2:
        await message.answer(" Пожалуйста, укажи название трека после команды /track")
        return

    tracks = search_by_track(args[1], limit=10)
    if not tracks:
        await message.answer(f"Трек '{args[1]}' не найден ")
        return

    text = f"🎵 Результаты поиска трека '{args[1]}':\n\n"
    for i, t in enumerate(tracks[:10], 1):
        text += f"{i}. 🎵 {t.get('trackName', 'Неизвестно')}\n"
        text += f"    {t.get('artistName', 'Неизвестно')}\n"
        if 'previewUrl' in t:
            text += f"   🔗 {t['previewUrl']}\n"
        text += "\n"
    await message.answer(text[:4000])


# --- Любой текст (поиск по общему запросу) ---
@dp.message()
async def music_search(message: Message):
    query = message.text.strip()
    if not query:
        return

    tracks = search_music(query, limit=10)

    if not tracks:
        await message.answer(f"По запросу '{query}' ничего не найдено \nПопробуй другой запрос или используй /help")
        return

    text = f"🎶 Результаты по запросу '{query}':\n\n"
    for i, t in enumerate(tracks[:10], 1):
        text += f"{i}. 🎵 {t.get('trackName', 'Неизвестно')}\n"
        text += f"    {t.get('artistName', 'Неизвестно')}\n"
        if 'previewUrl' in t:
            text += f"    {t['previewUrl']}\n"
        text += "\n"

    await message.answer(text[:4000])


# --- Запуск бота ---
async def main():
    print("Бот запущен! Ищет музыку...")
    await dp.start_polling(bot)


if __name__ == "__main__":
    asyncio.run(main())
```

### Подключаем библиотеки

`import requests from aiogram import Bot, Dispatcher, types`

`requests` — чтобы сходить на сайт и спросить музыку
    
`aiogram` — чтобы бот умел читать и отправлять сообщения
    

---
### Токен

`TOKEN = "8205786674:AAF0JYnQBU7F6-hXQ0eqjYoJZyxAFhlxKsA"`

Это мой токен для работы бота
Telegram по нему понимает, какой бот работает.

---
### Создаём бота

`bot = Bot(token=TOKEN) dp = Dispatcher()`

`bot` — отправляет сообщения

`dp` — принимает сообщения

---

### Функция поиска

`def search_music(query):`

Эта функция:
получает текст (что ты написал)
ищет музыку
возвращает песни
    
---
`params = {     "term": query,     "entity": "song",     "limit": 5 }`

Здесь мы говорим сайту:

искать по этому тексту
    
искать именно песни
    
вернуть не больше 5 штук
    
---

`return requests.get(url, params=params).json()["results"]`

Проще:

 «Сходи на сайт → возьми ответ → верни песни»

---
### Когда ты пишешь сообщение

`@dp.message() async def music(message):`

Это означает:

«Если кто-то напишет боту — запускай этот код»

---

`tracks = search_music(message.text)`

Ищет по нему музыку

---
### Если ничего не нашли

`if not tracks:     await message.answer("Ничего не найдено 😔")`

Бот честно скажет, что песен нет

---

### Готовим ответ

`text = "🎶 Результаты:\n\n"`

Создаём текст, который отправим

---

`for t in tracks:`

Берём каждую найденную песню

---

`text += (     f"🎵 {t['trackName']}\n"     f"👤 {t['artistName']}\n"     f"{t['previewUrl']}\n\n" )`

Добавляем:

название песни
    
исполнителя
    
---
### Отправляем сообщение

`await message.answer(text)`

Бот отправляет готовый текст тебе

---
### Запуск бота

`dp.run_polling(bot)`

бот запускается